#include<bits/stdc++.h>
using namespace std;

int beauty(vector<int> &A,int size)
{
    int a,b,c,d;
}

int main()
{
    int n;
    cin>>n;
    vector<int> ind;
    vector<vector<int>> v;
    for (int i = 0; i < n; i++)
    {
        int p;
        cin>>p;
        int.push_back(p);
        for (int j = 0; j < p; j++)
        {
            int t;
            cin>>t;
            v[i][j]=t;
        }
        
        
    }
    
    return 0;
}